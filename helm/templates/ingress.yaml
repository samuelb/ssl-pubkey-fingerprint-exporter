{{- if .Values.ingress.enabled -}}
{{- $fullName := include "ssl-pubkey-fingerprint-exporter.fullname" . -}}
{{- $apiVersion := include "ssl-pubkey-fingerprint-exporter.ingress.apiVersion" . -}}
apiVersion: {{ $apiVersion }}
kind: Ingress
metadata:
  name: {{ $fullName }}
  labels:
    app.kubernetes.io/name: {{ include "ssl-pubkey-fingerprint-exporter.name" . }}
    helm.sh/chart: {{ include "ssl-pubkey-fingerprint-exporter.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  {{- with .Values.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
{{- if .Values.ingress.tls }}
  tls:
  {{- range .Values.ingress.tls }}
    - hosts:
      {{- range .hosts }}
        - {{ . | quote }}
      {{- end }}
      secretName: {{ .secretName }}
  {{- end }}
{{- end }}
  rules:
  {{- range .Values.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
        {{- range .paths }}
          - path: {{ . }}
            {{- if eq $apiVersion "networking.k8s.io/v1" }}
            pathType: {{ $.Values.ingress.pathType | default "ImplementationSpecific" }}
            backend:
              service:
                name: {{ $fullName }}
                port:
                  name: http
            {{- else }}
            backend:
              serviceName: {{ $fullName }}
              servicePort: http
            {{- end }}
        {{- end }}
  {{- end }}
{{- end }}
